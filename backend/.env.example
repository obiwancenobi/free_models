# OpenRouter API Configuration
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Server Configuration
PORT=3001
NODE_ENV=development
HOST=0.0.0.0  # Bind to all interfaces for container networking

# CORS Configuration (for development)
ALLOWED_ORIGINS=http://localhost:3000

# SECURITY NOTE: In production with Docker, backend APIs are only accessible
# within the same Docker network. No ports are exposed to the host machine.
# CORS restricts origins to allowed domains only.

# Database Configuration
DATABASE_URL=postgresql://username:password@hostname:port/database_name
DB_SSL=require
DB_MAX_CONNECTIONS=10